apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: longsocks

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml

secretGenerator:
  - name: longsocks-tokens
    envs:
      - .env.tokens
